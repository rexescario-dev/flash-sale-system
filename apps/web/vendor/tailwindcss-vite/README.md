# Local `@tailwindcss/vite` bridge (#122)

No-op Vite plugin so `tailwindcss()` registration matches the app entrypoint while npm registry access is blocked. Real plugin + utility generation land with official packages in **#133**.
