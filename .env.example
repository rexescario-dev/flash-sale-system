# Development configuration. Copy to `.env` for local runs.
# Never commit real secrets.

PORT=3000
NODE_ENV=development

# Web (VITE_* values are exposed to the browser — never put secrets here)
VITE_API_URL=http://localhost:3000
